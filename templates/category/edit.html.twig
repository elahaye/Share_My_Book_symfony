{% extends 'base.html.twig' %}

{% block title %}Modification d'une catégorie{% endblock %}

{% block body %}

<div class="w-auto m-3 p-2">
    <div class="m-auto card border-warning mb-3" style="max-width: 30rem;">
        <h1 class="card-header">Modification d'une catégorie existante</h1>
        <div class="card-body">
            <div class="m-3 p-2">
                {{ form_start(form) }}
                {{ form_widget(form) }}
                    <button type="submit" class="btn btn-warning mt-2">Modifier</button>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
</div>

{% endblock %}